_subdirectory: template

directoryName:
    type: str
    help: What is the directory name of the new project? (Use [0-9]{4})
moduleName:
    type: str
    help: What is the name of the module? (Use [A-Z][A-Za-z0-9_]*)
